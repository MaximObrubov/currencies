<% if flash.present? %>
  <div class="flashes pt-3">
    <% flash.each do |type, message| %>
    <div class="alert <%= bootstrap_class_for(type) %>">
      <button class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
    <% end %>
  </div>
  <% flash.clear %>
<% end %>
